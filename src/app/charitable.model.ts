export class Charitable {
  constructor(public name: string, public target: number, public funded: number, public pledged: number, public shortInfo: string, public longInfo: string, public imageUrl: string, public deductable: boolean, public beneficiary: string)
}
